---
description: Show running and recent Statistica research jobs
argument-hint: "[job-id]"
allowed-tools: Bash(node:*)
---

Show Statistica research jobs for the current directory (or one job by id).

Run:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/companion.mjs" status "$ARGUMENTS"
```

Return the output verbatim.
